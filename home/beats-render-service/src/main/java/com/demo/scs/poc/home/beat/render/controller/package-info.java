/**
 * Some controller that render different templates and return a fragment of the page. To decouple data passed via post each
 * controller has an own class used to put the data into the template.
 */
package com.demo.scs.poc.home.beat.render.controller;
